#include <stdio.h>

/**
  * main - All the single digit number from 0 to 10
  *
  * Return: Always0 (succeed)
  */
int main(void)
{
	int a;
	
	for (a == 0; a <= 9; a++)
	{
		printf("%d",a);
	}

	printf("\n");
	return (0);
}
