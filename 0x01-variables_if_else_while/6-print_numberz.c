#include <stdio.h>
/**
  * main prints all single digit numbers of base 10 starting from 0
  * 
  * Return: Always 0 (succeeds)
  */
int main(void)
{
	int a;
	for (a = 48; a <= 57; a ++)
	{
		putchar(a);
	}
	putchar('\n');
	return (0);
}
